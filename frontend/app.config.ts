export default defineAppConfig({
    ui: {
        primary: "currentColor",
        button: {
            default: {
                color: 'primary'

            }
        },
        select: {
            default: {
                rounded: 'rounded-lg',
            }
        },
    }
})